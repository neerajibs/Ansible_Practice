---
- hosts: all
  become: true
  become_user: root
  tasks:
   - name: show the ansible
     debug: 

         msg: 
                   The Default address of {{ ansible_fqdn }} is {{ ansible_all_ipv4_addresses }}
